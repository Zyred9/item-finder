# 寻物记 (Item Finder) - 产品需求文档 v3.0

**文档状态：** 待评审  
**版本：** v3.0（对话 + 表单混合模式）  
**创建日期：** 2026-03-03  
**最后更新：** 2026-03-03  
**负责人：** 小红

---

## 目录

1. [产品概述](#一产品概述)
2. [产品架构](#二产品架构)
3. [首页设计](#三首页设计)
4. [存物功能](#四存物功能)
5. [找物功能](#五找物功能)
6. [智能提醒](#六智能提醒)
7. [数据模型](#七数据模型)
8. [待确认问题](#八待确认问题)

---

## 一、产品概述

### 1.1 产品信息

| 字段 | 内容 |
|------|------|
| **产品名称** | 寻物记 / Item Finder |
| **产品形态** | 微信小程序 |
| **产品 Slogan** | 存得明白，找得轻松 |
| **版本号** | v3.0 |

### 1.2 产品愿景

**短期（MVP）：** 帮助家庭快速记录物品位置，减少寻找时间

**长期：** 成为每个家庭的"物品记忆库"，支持智能提醒、过期预警、物品保险等增值服务

### 1.3 核心价值

| 价值类型 | 描述 | 量化指标 |
|----------|------|----------|
| **时间价值** | 减少找东西的时间 | 每次节省 5-10 分钟 |
| **情绪价值** | 减少因找不到东西产生的焦虑和家庭矛盾 | 用户满意度 > 80% |
| **经济价值** | 避免重复购买（找不到以为丢了）+ 避免过期浪费 | 户均避免浪费 ¥500/年 |

### 1.4 产品定位

**核心交互模式：**
- **存物** → 表单方式（高效、结构化）
- **找物** → 对话方式（自然、灵活）
- **首页** → 信息聚合（最近存放 + 智能提醒）

**目标用户：**
- 核心用户：家庭主妇/主夫 + 上班族（25-45 岁）
- 边缘用户：老人（55+ 岁）、青少年（10-18 岁）

---

## 二、产品架构

### 2.1 整体架构

```
┌─────────────────────────────────────┐
│           底部 Tab 导航              │
├─────────────────────────────────────┤
│  🏠 首页  │  📦 存物  │  💬 找物  │  👤 我的  │
└─────────────────────────────────────┘
```

### 2.2 功能模块

| Tab | 模块 | 核心功能 | 优先级 |
|-----|------|----------|--------|
| **🏠 首页** | 信息聚合 | 搜索框、快捷入口、智能提醒、最近存放 | P0 |
| **📦 存物** | 物品录入 | 表单填写、拍照、语音输入、分类扩展字段 | P0 |
| **💬 找物** | 物品查找 | 对话式搜索、多轮问答、结果展示 | P0 |
| **👤 我的** | 个人设置 | 家庭管理、成员管理、设置、帮助 | P1 |

### 2.3 页面结构

```
item-finder/
├── pages/
│   ├── index/          # 首页
│   │   ├── index.js
│   │   ├── index.json
│   │   ├── index.wxml
│   │   └── index.wxss
│   │
│   ├── store/          # 存物页
│   │   ├── store.js
│   │   ├── store.json
│   │   ├── store.wxml
│   │   └── store.wxss
│   │
│   ├── chat/           # 找物对话页
│   │   ├── chat.js
│   │   ├── chat.json
│   │   ├── chat.wxml
│   │   └── chat.wxss
│   │
│   ├── detail/         # 物品详情页
│   │   ├── detail.js
│   │   ├── detail.json
│   │   ├── detail.wxml
│   │   └── detail.wxss
│   │
│   ├── family/         # 家庭管理页
│   │   ├── family.js
│   │   ├── family.json
│   │   ├── family.wxml
│   │   └── family.wxss
│   │
│   └── profile/        # 个人中心页
│       ├── profile.js
│       ├── profile.json
│       ├── profile.wxml
│       └── profile.wxss
│
├── components/         # 自定义组件
│   ├── item-card/      # 物品卡片
│   ├── reminder-card/  # 提醒卡片
│   └── category-picker/# 分类选择器
│
└── utils/              # 工具函数
    ├── api.js
    ├── auth.js
    └── util.js
```

---

## 三、首页设计

### 3.1 页面布局

```
┌─────────────────────────────────┐
│  🏠 张家               👤 我的   │  ← 导航栏
├─────────────────────────────────┤
│                                 │
│  ┌───────────────────────────┐  │
│  │ 🔍 搜索物品...             │  │  ← 全局搜索框
│  └───────────────────────────┘  │
│                                 │
│  ┌───────────┬───────────┐     │
│  │   📦      │    💬     │     │
│  │  快速存物 │  快速找物  │     │  ← 快捷入口
│  └───────────┴───────────┘     │
│                                 │
│  ⚠️ 智能提醒              [全部] │  ← 智能提醒区
│  ┌───────────────────────────┐  │
│  │ 🔴 紧急（3）               │  │
│  │ ┌─────────────────────┐   │  │
│  │ │ 💊 感冒药  还有 3 天过期  │   │  │
│  │ │    [处理] [忽略]      │   │  │
│  │ └─────────────────────┘   │  │
│  │ ┌─────────────────────┐   │  │
│  │ │ 🥛 酸奶    还有 1 天过期  │   │  │
│  │ │    [处理] [忽略]      │   │  │
│  │ └─────────────────────┘   │  │
│  └───────────────────────────┘  │
│                                 │
│  🕐 最近存放                    │
│  ┌───────────────────────────┐  │
│  │ ┌───┐                     │  │
│  │ │📷 │ 吹风机              │  │
│  │ │   │ 主卧抽屉 · 张三     │  │
│  │ └───┘ 10:30               │  │
│  ├───────────────────────────┤  │
│  │ ┌───┐                     │  │
│  │ │📷 │ 护照                │  │
│  │ │   │ 保险柜 · 李四       │  │
│  │ └───┘ 昨天                │  │
│  └───────────────────────────┘  │
│                                 │
├─────────────────────────────────┤
│  🏠      📦       💬      👤     │
│  首页    存物     找物    我的   │  ← 底部 Tab
└─────────────────────────────────┘
```

### 3.2 模块说明

#### 3.2.1 搜索框
- **功能：** 全局搜索物品
- **交互：** 点击跳转到搜索页面
- **支持：** 物品名、位置、描述模糊匹配

#### 3.2.2 快捷入口
| 按钮 | 功能 | 跳转 |
|------|------|------|
| 📦 快速存物 | 快速进入存物表单 | `/pages/store/store` |
| 💬 快速找物 | 快速进入对话找物 | `/pages/chat/chat` |

#### 3.2.3 智能提醒
- **展示：** 按紧急程度排序（紧急 > 待处理 > 一般）
- **操作：** [处理] [延期] [忽略]
- **跳转：** 点击 [全部] 查看完整提醒列表

#### 3.2.4 最近存放
- **数量：** 展示最近 10 条
- **排序：** 按创建时间倒序
- **内容：** 物品名 + 位置 + 存放人 + 时间
- **交互：** 点击查看详情

---

## 四、存物功能

### 4.1 页面布局

```
┌─────────────────────────────────┐
│  ← 存物                  [保存]  │
├─────────────────────────────────┤
│                                 │
│  📷 拍照/选图                    │
│  ┌───────────────────────────┐  │
│  │                           │  │
│  │      [📷 拍照/选图]        │  │
│  │                           │  │
│  └───────────────────────────┘  │
│                                 │
│  🎤 语音输入                     │
│  ┌───────────────────────────┐  │
│  │ [🎤 按住说话]              │  │
│  │ "吹风机放主卧抽屉"         │  │
│  └───────────────────────────┘  │
│                                 │
│  物品名 *                        │
│  ┌───────────────────────────┐  │
│  │ 吹风机                     │  │
│  └───────────────────────────┘  │
│                                 │
│  存放位置 *                      │
│  ┌───────────────────────────┐  │
│  │ 主卧抽屉                   │  │
│  └───────────────────────────┘  │
│                                 │
│  物品分类                        │
│  ┌───────────────────────────┐  │
│  │ 请选择分类    ▼            │  │
│  └───────────────────────────┘  │
│                                 │
│  描述（可选）                    │
│  ┌───────────────────────────┐  │
│  │ 红色，戴森品牌...          │  │
│  └───────────────────────────┘  │
│                                 │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  📋 扩展信息（根据分类显示）     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                 │
│  [选择分类后显示对应字段]        │
│                                 │
└─────────────────────────────────┘
```

### 4.2 基础字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 照片 | Image | 否 | 支持拍照/相册选择 |
| 语音输入 | Audio | 否 | 按住说话，自动转文字 |
| 物品名 | Text | 是 | 1-50 字 |
| 存放位置 | Text | 是 | 1-100 字 |
| 物品分类 | Select | 否 | 选择后显示扩展字段 |
| 描述 | Textarea | 否 | 0-200 字 |

### 4.3 物品分类体系

```
物品分类
├── 🏠 家居用品
│   ├── 清洁用品
│   ├── 收纳用品
│   └── 装饰品
├── 🔌 电器数码
│   ├── 厨房电器
│   ├── 生活电器
│   ├── 数码产品
│   └── 配件
├── 💊 药品健康
│   ├── 处方药
│   ├── 非处方药
│   ├── 保健品
│   └── 医疗器械
├── 🍔 食品饮料
│   ├── 零食
│   ├── 调味品
│   ├── 保健品
│   └── 婴幼儿食品
├── 📄 证件文件
│   ├── 身份证
│   ├── 护照
│   ├── 银行卡
│   ├── 合同
│   └── 票据
├── 👕 服饰鞋包
│   ├── 上衣
│   ├── 裤子
│   ├── 鞋子
│   └── 包包
├── 🧸 母婴用品
│   ├── 奶粉
│   ├── 尿布
│   ├── 玩具
│   └── 洗护
└── 📦 其他
```

### 4.4 扩展字段（按分类）

#### 4.4.1 药品类
```
┌─────────────────────────────────┐
│  💊 药品信息                     │
├─────────────────────────────────┤
│  药品名称 *                      │
│  ┌───────────────────────────┐  │
│  │ 感冒药                     │  │
│  └───────────────────────────┘  │
│                                 │
│  有效期 *                        │
│  ┌───────────────────────────┐  │
│  │ 选择过期日期    📅 2026-12-31│  │
│  └───────────────────────────┘  │
│                                 │
│  开封日期（可选）                │
│  ┌───────────────────────────┐  │
│  │ 选择开封日期    📅 2026-03-01│  │
│  └───────────────────────────┘  │
│                                 │
│  开封后有效期（天）              │
│  ┌───────────────────────────┐  │
│  │ 30                        │  │
│  └───────────────────────────┘  │
│                                 │
│  用药提醒（可选）                │
│  ┌───────────────────────────┐  │
│  │ 每日 3 次，每次 1 片          │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

#### 4.4.2 食品类
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 食品名称 | Text | 是 | - |
| 生产日期 | Date | 否 | 用于计算保质期 |
| 保质期（天）| Number | 否 | - |
| 储存条件 | Select | 否 | 冷藏/常温/冷冻 |

#### 4.4.3 证件类
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 证件名称 | Text | 是 | - |
| 证件号码 | Text | 否 | 加密存储 |
| 有效期 | Date | 否 | 到期提醒 |
| 发证机关 | Text | 否 | - |

#### 4.4.4 电器类
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 电器名称 | Text | 是 | - |
| 品牌 | Text | 否 | - |
| 型号 | Text | 否 | - |
| 购买日期 | Date | 否 | - |
| 保修期至 | Date | 否 | 保修到期提醒 |
| 配件清单 | Textarea | 否 | - |

#### 4.4.5 衣物类
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 衣物名称 | Text | 是 | - |
| 尺码 | Text | 否 | - |
| 颜色 | Text | 否 | - |
| 季节 | Select | 否 | 春/夏/秋/冬 |
| 材质 | Text | 否 | - |

### 4.5 语音输入功能

**交互流程：**
```
1. 用户点击 [🎤 按住说话]
2. 开始录音，显示"正在录音..."
3. 用户松开，录音结束
4. 调用语音识别 API，转成文字
5. 自动填充到表单字段：
   - 识别出物品名 → 填充"物品名"
   - 识别出位置 → 填充"存放位置"
   - 识别出其他信息 → 填充"描述"
6. 用户确认/修改后保存
```

**语音识别示例：**
```
用户说："感冒药放药箱上层，2026 年 12 月过期"

识别结果：
- 物品名：感冒药
- 位置：药箱上层
- 描述：2026 年 12 月过期
- 分类建议：药品健康（自动选择）
```

### 4.6 保存逻辑

**验证规则：**
- 物品名：必填，1-50 字
- 存放位置：必填，1-100 字
- 有效期：药品类必填

**保存后：**
- 提示"保存成功"
- 返回首页
- 更新"最近存放"列表

---

## 五、找物功能

### 5.1 对话界面

```
┌─────────────────────────────────┐
│  💬 找物助手            🗑️ 清空 │
├─────────────────────────────────┤
│                                 │
│  AI: 你好！我是寻物记助手，     │
│      帮你找东西～               │
│                                 │
│  用户：我的护照在哪？           │
│                                 │
│  AI: 📄 护照在保险柜第二层，    │
│      是张三于 3 月 1 日存放的。    │
│      ┌──────────┬──────────┐   │
│      │ 📍 导航   │ 📷 查看照片│   │
│      └──────────┴──────────┘   │
│                                 │
│  用户：那吹风机呢？             │
│                                 │
│  AI: 🔌 吹风机有两个记录：      │
│      1. 主卧抽屉（张三，3 月 1 日）│
│      2. 卫生间柜子（李四，2 月）  │
│      你要找哪个？               │
│                                 │
│  用户：第一个                   │
│                                 │
│  AI: ✅ 好的，主卧抽屉的吹风机  │
│      需要我导航过去吗？         │
│                                 │
├─────────────────────────────────┤
│  📷  [🎤 按住说话]  ⌨️  发送   │
└─────────────────────────────────┘
```

### 5.2 对话场景

#### 场景 1：精确查找
```
用户："我的护照在哪？"
AI：[查询数据库]
    "📄 护照在保险柜第二层，是张三于 3 月 1 日存放的。"
    [📍 导航] [📷 查看照片] [🔊 播报]
```

#### 场景 2：模糊查找
```
用户："感冒药在哪？"
AI：[模糊匹配]
    "💊 找到 3 个感冒药：
     1. 999 感冒灵 - 药箱上层（昨天）
     2. 泰诺 - 床头柜（上周）
     3. 连花清瘟 - 客厅药箱（上月）
     你要找哪个？"
```

#### 场景 3：位置反查
```
用户："主卧抽屉里有什么？"
AI：[按位置查询]
    "主卧抽屉里有 5 样东西：
     1. 吹风机
     2. 指甲刀
     3. 备用眼镜
     4. 充电器
     5. 钱包
     要查看哪个的详情？"
```

#### 场景 4：过期查询
```
用户："有什么东西快过期了？"
AI：[查询有效期]
    "⚠️ 有 3 样东西快过期了：
     1. 感冒药 - 还剩 3 天（3 月 6 日）
     2. 酸奶 - 还剩 1 天（3 月 4 日）
     3. 护照 - 还剩 5 个月（8 月 1 日）
     需要我帮你处理吗？"
```

#### 场景 5：多轮对话
```
用户："我的钥匙在哪？"
AI："🔑 找到 3 把钥匙：
     1. 大门钥匙 - 玄关鞋柜（张三）
     2. 车钥匙 - 电视柜抽屉（李四）
     3. 抽屉钥匙 - 书房书桌（张三）
     你要找哪把？"

用户："大门的"
AI："✅ 大门钥匙在玄关鞋柜，是张三于 2 月 28 日存放的。
     需要我导航过去吗？"

用户："好的"
AI："📍 已打开地图导航"
```

### 5.3 对话能力

| 能力 | 说明 | 技术实现 |
|------|------|----------|
| **意图识别** | 识别用户要找什么 | 关键词匹配 + LLM |
| **模糊搜索** | 支持同义词、近义词 | 数据库 LIKE 查询 |
| **多轮对话** | 支持上下文理解 | 会话状态管理 |
| **结果展示** | 文字 + 图片 + 操作按钮 | 富文本消息 |
| **语音播报** | 语音回复用户 | 百度 TTS |

### 5.4 对话历史

- **保存：** 每次对话保存 30 天
- **清空：** 用户可手动清空
- **隐私：** 对话内容不上传云端

---

## 六、智能提醒

### 6.1 提醒类型

| 类型 | 触发条件 | 提醒时机 | 示例 |
|------|----------|----------|------|
| **过期提醒** | 物品有有效期 | 过期前 7 天、3 天、1 天、当天 | "感冒药还有 3 天过期" |
| **开封提醒** | 物品开封后有保质期 | 开封后到期前 3 天、1 天 | "奶粉开封超过 1 个月" |
| **保修提醒** | 电器保修期快到期 | 保修到期前 30 天、7 天 | "空调保修还有 15 天到期" |
| **证件提醒** | 证件有效期不足 | 到期前 6 个月、3 个月、1 个月 | "护照有效期不足 6 个月" |
| **长期未用** | 物品超过 X 天未使用 | 可配置（如 90 天） | "电钻已 90 天未使用" |
| **自定义提醒** | 用户手动设置 | 指定日期 | "明天记得带合同" |

### 6.2 提醒级别

| 级别 | 颜色 | 场景 | 推送方式 |
|------|------|------|----------|
| **🔴 紧急** | 红色 | 过期前 1 天、当天过期 | 首页 + 服务通知 |
| **🟡 待处理** | 黄色 | 过期前 3-7 天 | 首页 |
| **🟢 一般** | 绿色 | 过期前 8-30 天 | 首页（折叠） |

### 6.3 提醒处理操作

| 操作 | 说明 |
|------|------|
| **处理** | 标记为已处理，从提醒列表移除 |
| **延期** | 延长提醒时间（如延期 7 天） |
| **忽略** | 忽略本次提醒，下次到期再提醒 |
| **删除** | 删除物品记录 |

### 6.4 提醒推送策略

| 渠道 | 场景 | 频率 |
|------|------|------|
| **首页提醒区** | 所有提醒 | 实时显示 |
| **微信服务通知** | 紧急提醒（过期前 3 天内） | 每天最多 1 次 |
| **找物对话** | 用户询问时主动告知 | 按需 |

---

## 七、数据模型

### 7.1 ER 图

```
┌─────────────┐       ┌─────────────┐
│  families   │       │    users    │
├─────────────┤       ├─────────────┤
│ id (PK)     │◄──────│ family_id   │
│ name        │       │ id (PK)     │
│ invite_code │       │ wechat_openid│
│ created_at  │       │ nickname    │
└─────────────┘       │ avatar_url  │
       ▲              │ is_admin    │
       │              │ created_at  │
       │              └─────────────┘
       │                     │
       │                     │
       │              ┌──────┴──────┐
       │              │             │
       ▼              ▼             │
┌─────────────┐ ┌─────────────┐    │
│    items    │ │ creator_id  │────┘
├─────────────┤ └─────────────┘
│ id (PK)     │
│ family_id   │
│ creator_id  │
│ name        │
│ location    │
│ category    │
│ ...         │
└─────────────┘
```

### 7.2 物品表结构

```sql
CREATE TABLE items (
    -- 基础信息
    id              TEXT PRIMARY KEY,
    family_id       TEXT NOT NULL,
    creator_id      TEXT NOT NULL,
    name            TEXT NOT NULL,           -- 物品名
    location        TEXT NOT NULL,           -- 存放位置
    description     TEXT,                    -- 描述
    photo_path      TEXT,                    -- 照片路径
    
    -- 分类
    category        TEXT,                    -- 物品分类
    subcategory     TEXT,                    -- 子分类
    
    -- 有效期相关
    expire_date     DATE,                    -- 过期日期
    production_date DATE,                    -- 生产日期
    shelf_life      INTEGER,                 -- 保质期（天）
    open_date       DATE,                    -- 开封日期
    open_shelf_life INTEGER,                 -- 开封后保质期（天）
    
    -- 证件相关
    document_number TEXT,                    -- 证件号码（加密）
    issuer          TEXT,                    -- 发证机关
    
    -- 电器相关
    brand           TEXT,                    -- 品牌
    model           TEXT,                    -- 型号
    purchase_date   DATE,                    -- 购买日期
    warranty_date   DATE,                    -- 保修到期日
    
    -- 衣物相关
    size            TEXT,                    -- 尺码
    color           TEXT,                    -- 颜色
    season          TEXT,                    -- 季节
    
    -- 状态
    status          TEXT DEFAULT 'active',   -- active/archived/deleted
    is_favorite     BOOLEAN DEFAULT FALSE,   -- 是否收藏
    
    -- 时间戳
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    last_found_at   TIMESTAMP,               -- 最后查找时间
    find_count      INTEGER DEFAULT 0,       -- 被查找次数
    
    -- 索引
    INDEX idx_family (family_id),
    INDEX idx_creator (creator_id),
    INDEX idx_category (category),
    INDEX idx_expire (expire_date),
    INDEX idx_status (status)
);
```

### 7.3 提醒表结构

```sql
CREATE TABLE reminders (
    id              TEXT PRIMARY KEY,
    item_id         TEXT REFERENCES items(id),
    family_id       TEXT NOT NULL,
    type            TEXT NOT NULL,           -- 提醒类型
    title           TEXT NOT NULL,           -- 提醒标题
    content         TEXT,                    -- 提醒内容
    remind_at       DATE NOT NULL,           -- 提醒时间
    triggered_at    TIMESTAMP,               -- 触发时间
    status          TEXT DEFAULT 'pending',  -- pending/done/ignored
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 八、已确认决策 ✅

> **确认时间：** 2026-03-03  
> **确认人：** 小红

### 决策 1：物品分类选择方式 → **混合模式**

**最终方案：AI 智能识别 + 用户可修改**

- 用户输入物品名后，AI 自动识别分类
- 识别结果展示在分类选择器中，用户可修改
- 识别准确率 > 80% 时直接选中，否则让用户手动选择

**实现细节：**
```javascript
// 存物页面逻辑
onItemNameInput(name) {
  // 1. 调用分类识别 API
  const category = await api.predictCategory(name)
  
  // 2. 自动选中（置信度 > 0.8）
  if (category.confidence > 0.8) {
    this.setData({ selectedCategory: category.id })
  }
  
  // 3. 显示推荐分类供用户修改
  this.setData({ suggestedCategory: category })
}
```

---

### 决策 2：特殊字段必填/可选 → **可选 + 引导提示**

**最终方案：所有扩展字段均为可选，但提供智能引导**

- 不强制填写任何扩展字段
- 根据分类智能提示建议填写内容
- 提供友好提示说明填写后的好处（如"填写过期日期可获得到期提醒"）

**实现细节：**
```javascript
// 分类字段配置示例
const categoryFields = {
  medicine: [
    { name: 'expire_date', label: '有效期', required: false, 
      hint: '💡 填写后可在过期前收到提醒' },
    { name: 'open_date', label: '开封日期', required: false,
      hint: '💡 部分药品开封后保质期会缩短' }
  ]
}
```

---

### 决策 3：提醒推送频率 → **关键节点推送**

**最终方案：仅在过期前 7 天、3 天、1 天、当天推送**

**推送策略：**
| 时间节点 | 推送级别 | 推送方式 |
|----------|----------|----------|
| 过期前 7 天 | 🟢 一般 | 首页提醒区 |
| 过期前 3 天 | 🟡 待处理 | 首页提醒区 + 微信服务通知 |
| 过期前 1 天 | 🔴 紧急 | 首页提醒区 + 微信服务通知 |
| 当天过期 | 🔴 紧急 | 首页提醒区 + 微信服务通知 |

**实现细节：**
```python
# 后端定时任务（每日凌晨执行）
def generate_reminders():
    today = datetime.now().date()
    
    # 查询即将过期的物品
    for item in items_with_expire_date:
        days_left = (item.expire_date - today).days
        
        if days_left in [7, 3, 1, 0]:
            create_reminder(
                item=item,
                type='expire',
                level='urgent' if days_left <= 1 else 'warning',
                remind_at=today
            )
```

---

### 决策 4：语音输入填充方式 → **自动填充 + 用户确认**

**最终方案：语音识别后自动填充表单各字段，用户确认后保存**

**处理流程：**
```
1. 用户按住说话："吹风机放主卧抽屉，红色戴森的"
2. 语音识别 → 文本
3. NLP 实体提取 → { item_name: "吹风机", location: "主卧抽屉", description: "红色戴森的" }
4. 自动填充表单各字段
5. 用户确认/修改后保存
```

**实现细节：**
```javascript
// 语音识别结果处理
onVoiceRecognized(result) {
  const { text, entities } = result
  
  // 自动填充表单
  this.setData({
    formData: {
      name: entities.item_name || '',
      location: entities.location || '',
      description: entities.description || text
    }
  })
  
  // 提示用户确认
  wx.showToast({
    title: '已自动填充，请确认',
    icon: 'none'
  })
}
```

---

## 九、开发优先级

### P0 - MVP 核心功能

| 模块 | 功能 | 工期 |
|------|------|------|
| **首页** | 搜索框、最近存放 | 2 天 |
| **存物** | 基础表单（物品名 + 位置 + 照片） | 3 天 |
| **找物** | 对话界面 + 基础搜索 | 3 天 |
| **家庭** | 创建/加入家庭 | 1 天 |
| **后端** | API + 数据库 | 3 天 |

**MVP 总计：** 约 2 周

### P1 - 增强功能

| 模块 | 功能 | 工期 |
|------|------|------|
| **存物** | 语音输入、分类扩展字段 | 3 天 |
| **找物** | 多轮对话、语音播报 | 3 天 |
| **首页** | 智能提醒 | 2 天 |
| **我的** | 家庭管理、设置 | 2 天 |

**P1 总计：** 约 2 周

### P2 - 优化功能

| 模块 | 功能 | 工期 |
|------|------|------|
| **智能** | AI 自动分类、智能推荐 | 3 天 |
| **提醒** | 微信服务通知推送 | 2 天 |
| **统计** | 物品统计、使用报告 | 2 天 |

**P2 总计：** 约 1 周

---

## 十、附录

### 10.1 竞品参考

| 产品 | 优势 | 劣势 | 参考点 |
|------|------|------|--------|
| 手机相册 | 无需安装 | 无法搜索 | - |
| Sortly | 功能丰富 | 需要下载、付费 | 物品管理逻辑 |
| 标签机 | 物理标签直观 | 需要购买设备 | - |

### 10.2 技术文档

- [微信小程序文档](https://developers.weixin.qq.com/miniprogram/dev/framework/)
- [FastAPI 文档](https://fastapi.tiangolo.com/)
- [百度语音 API](https://ai.baidu.com/tech/speech)

### 10.3 变更日志

| 版本 | 日期 | 变更内容 | 负责人 |
|------|------|----------|--------|
| v1.0 | 2026-03-02 | 初始版本（表单模式） | 小红 |
| v2.0 | 2026-03-03 | 对话式 AI 助手 | 小红 |
| v3.0 | 2026-03-03 | 存物表单 + 找物对话混合模式 | 小红 |

---

**PRD v3.0 完成！** 🎉

**下一步：**
1. 确认待确认问题（第八章）
2. 输出高保真原型
3. 开始编码实现

---

**文档位置：** `C:\Users\zyred\.copaw\workspaces\item-finder\PRD_V3.md`
